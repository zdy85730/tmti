# META-TI 交互与界面规范

## 文档目的

这份规范只回答一件事：

`如何不靠解释和说教，仅通过交互、层级、默认态和动效，让用户自己意识到“结果并不只有封面这一层”。`

这不是理念文档。

这是一份可直接用于产品、设计、前端协作的界面规范。

## 总体原则

### 1. 不明说

不要在页面上直接出现下面这类表达：

- 你真正的样子
- 这是假的结果
- 系统在替你筛选
- 你更愿意认领这一版
- 你只是想发一个体面的标签

这些话都不应该由页面说出来。

### 2. 让用户自己察觉“有差异”

反转不靠解释，靠四种不对称：

- 完成度不对称
- 默认态不对称
- 信息形态不对称
- 可导出性不对称

### 3. 第一层先成立，第二层再漏出来

如果第一层不够完整、不够顺手，用户不会认真看后面的内容。

所以必须先让封面卡成立，再让底下那层慢慢出现。

### 4. 第二层不是另一张标签卡

第二层不能长成另一张完整人格结果卡。

否则产品仍然在生产第二个标签，只是从 `Public Type` 变成 `Private Type`。

第二层必须是：

- 留痕
- 残留
- 底稿
- 批注
- 片段

而不是另一个“类型”。

## 页面总结构

结果页采用三层结构：

### 第一层：封面卡

作用：

- 负责认领
- 负责停留
- 负责传播

特征：

- 最完整
- 最亮
- 主标题最大
- 视觉最强
- 默认处于前景
- 默认参与导出

### 第二层：底稿卡

作用：

- 不负责认领
- 不负责传播
- 只负责让用户意识到“封面下面还有东西”

特征：

- 更灰
- 更轻
- 更像未完成物
- 不完整命名
- 默认不参与导出
- 初始不完整可见

### 第三层：导出区

作用：

- 把“分享”从功能变成选择动作

特征：

- 默认只导出封面卡
- 底稿卡需要手动带出
- 用户通过导出动作意识到“这不是唯一版本”

## 品牌显隐规范

### 目标

产品表层统一叫：

`TMTI`

`META-TI` 不作为首页品牌直接出现，而是作为结果结构被发现后的第二层命名。

### 默认规则

以下页面只允许出现 `TMTI`：

- 首页
- 答题页
- 等待页
- 结果页初始状态

### 允许露出 `META-TI` 的时机

只有在用户已经看到第二层痕迹之后，才允许轻微露出 `META-TI`。

允许位置：

- 结果页角落的小型字标
- 底稿卡边缘的极小注记
- 导出预览页眉
- 页面 title 的后半段

不允许位置：

- 首页主标题
- 主按钮
- 大段介绍文案
- 第一屏主视觉

### 露出方式

`META-TI` 的出现必须像“页面自己露出了一层名字”，而不是“产品正式改名”。

推荐：

- `TMTI` 字标在结果页停留后轻微扩展出 `META`
- 导出预览里出现小字 `META-TI`
- 底稿卡边缘出现极小的 `meta-ti`

不推荐：

- 弹窗通知“这其实是 META-TI”
- 大标题从 `TMTI` 硬切到 `META-TI`
- 单独一段解释 `TMTI` 和 `META-TI` 的关系

## 结果页布局规范

### 版式

采用前后叠层结构，而不是上下平铺的双卡结构。

推荐顺序：

1. 封面卡位于画面中心偏上
2. 底稿卡藏在封面卡后侧、下侧或右下侧
3. 底稿卡初始只露出 15% 到 25%
4. 用户停留、滑动或尝试导出时，底稿卡再进一步显露

禁用方案：

- 两张卡并排等权展示
- 两张卡上下完整排列
- 两张卡拥有相同视觉权重

原因：

这些布局会让用户直接理解成“系统给了我两个结果”，从而回到标签逻辑。

### 层级

封面卡必须具备明确主视觉权：

- 彩色背景
- 完整标题
- 大面积留白
- 明显主按钮
- 可截图区域完整

底稿卡必须具备明确次级权：

- 去饱和
- 对比度更低
- 内容更碎
- 没有主按钮
- 没有完整主标题

### 距离

封面卡与底稿卡不应完全分离。

推荐让两者有视觉重叠，保持“压住”和“漏出”的关系。

理想感觉不是“第二张卡在旁边”，而是：

`这张封面底下压着另一层没清干净的内容。`

## 封面卡规范

### 目的

封面卡必须先让用户产生：

`这个结果说得过去，而且能发。`

### 必备字段

- 标签名
- 一句短定义
- 一句副标题
- 一张视觉结果图或图形卡面
- 一个主导出按钮

### 字段要求

#### 标签名

- 2 到 6 个字为宜
- 能被快速复述
- 有体面感
- 不要太像病症
- 不要过度抽象

#### 短定义

- 14 到 26 字
- 像一句概括
- 允许模糊，但不能空

#### 副标题

- 适合截图传播
- 看起来像“有说法”
- 不要太像作者观点

### 禁止项

- 直接解释“这是你愿意展示的版本”
- 直接暗示“封面不是真相”
- 在封面卡上抢跑反转

封面卡必须先完整成立。

## 底稿卡规范

### 定义

底稿卡不是“另一张结果卡”，而是“未上封面的残留层”。

### 目标体验

用户看到它时，不应觉得：

- 系统又给了我一个更深的标签

而应觉得：

- 这张封面下面，好像还压着另一层没被放上来的内容

### 命名规范

允许使用的标题：

- 未导出的版本
- 另一种读法
- 保留片段
- 未上封面的部分
- 侧写留痕

不允许使用的标题：

- 真实的你
- 私下的你
- 隐藏人格
- 第二人格
- 真正结果

### 必备字段

底稿卡只允许包含以下元素：

- 一个非结论式标题
- 2 到 4 条片段化描述
- 1 条极短系统注释

不允许包含：

- 完整标签名
- 完整人格定义
- 独立主视觉
- 独立分享按钮
- 强烈主标题层级

### 片段化描述格式

每条片段应当像“被保留下来的证据”，而不是“新的人格判断”。

推荐格式：

- 更快接受：____
- 多次回避：____
- 未进入封面：____
- 保留描述：____
- 没被放大的部分：____

示例：

- 更快接受：克制、稳定、边界清楚
- 多次回避：需要回应、怕被误解、想确认关系
- 未进入封面：情绪起伏会影响判断

### 系统注释格式

只能是极轻的一句。

推荐方向：

- 该部分未进入默认展示
- 当前页面未展示全部内容
- 这部分被保留下来，但没有上封面
- 导出时默认忽略这一层

禁止方向：

- 这才是真实的你
- 你不愿意面对这一面
- 系统已经看穿你

## 系统留痕区规范

### 作用

系统留痕区是第三层，不负责命名，只负责把“封面为什么成立”留下痕迹。

### 形式

采用短句列表，每条不超过 22 字。

推荐数量：

- 3 条最佳
- 最多不超过 4 条

### 内容类型

只允许出现这三类信息：

#### 1. 接受速度

例如：

- 你对这一版的停留更久
- 你更快接受了偏体面的描述

#### 2. 用词偏好

例如：

- 你更倾向认领“克制”而不是“需要回应”
- 你反复选择了更容易公开使用的说法

#### 3. 暴露回避

例如：

- 高暴露选项多次未被选中
- 与关系需求有关的描述没有进入封面

### 禁止内容

- 长段解释
- 因果分析
- 心理教育
- 作者评论

系统留痕必须像日志，不像评语。

## 动画规范

### 总体目标

动画不是为了增强炫酷感，而是为了让用户感到：

- 封面先被摆上来
- 底稿后被漏出来
- 分享动作是在选择“带不带走底下那层”

### 结果出现顺序

推荐时序：

1. 页面进入
2. 封面卡先完成出现
3. 停顿 800ms 到 1200ms
4. 底稿卡边角轻微露出
5. 用户继续停留或滑动时，底稿卡再上浮 8px 到 16px
6. 品牌角标在非中心区域轻微从 `TMTI` 漏出 `META-TI`

关键点：

- 底稿卡不能和封面卡同时完整出现
- 底稿卡的第一步露出要像“压不住了”，不是“正式登场”
- `META-TI` 的露出必须晚于底稿卡第一次露出

### 触发方式

底稿卡露出可以由以下任一动作触发：

- 结果页停留超过 1.2 秒
- 用户下滑
- 用户点击导出前预览
- 用户长按封面卡

不推荐：

- 按钮文字写“查看另一面”
- 明确写“点此查看真实结果”

这些都太重，也太像讲解。

### 动效气质

推荐：

- 缓慢
- 轻微
- 低噪声
- 像层级位移

不推荐：

- 翻牌
- 爆闪
- 大面积弹窗
- 强提醒红点

因为这些都会把第二层做成“剧情揭晓”，而不是“痕迹浮现”。

## 导出区规范

### 目标

导出区要让用户自己意识到：

`当前可传播的是封面，但底稿也存在。`

### 默认态

默认导出预览中：

- 封面卡为选中状态
- 底稿卡为未选中状态
- 导出按钮默认只对应封面卡

这里不需要解释原因。

默认值本身就在说话。

### 允许动作

建议只保留三个动作：

- 发这一版
- 连同留痕一起导出
- 先留着

不建议使用：

- 分享真实结果
- 查看本质分析
- 暴露另一面

### 导出预览

当用户点击“连同留痕一起导出”时：

- 底稿卡才完整显示在导出预览里
- 但仍保持灰度和次级层级
- 不允许底稿卡变成主卡

原因：

即使被导出，底稿也不能升级为主结果。

## 默认态规范

为了让用户“找出来”而不是“被告诉”，默认态必须足够明确。

以下默认关系必须固定：

- 封面卡默认完整显示
- 底稿卡默认部分遮挡
- 封面卡默认参与分享
- 底稿卡默认不参与分享
- 主按钮默认服务封面卡
- 第二层内容默认需要停留后才看到
- 产品默认品牌态为 `TMTI`
- `META-TI` 默认不可见

这些默认态不能被文案代替。

## 视觉规范

### 封面卡

- 高饱和或明确品牌色
- 强对比
- 完整构图
- 可独立截图

### 底稿卡

- 灰阶或低饱和
- 更像纸面残留
- 更偏文本和短语
- 禁止强插画

### 留痕区

- 采用系统日志感排版
- 信息轻
- 字号小于主卡
- 不抢视觉中心

## 禁止事项

以下做法会让产品重新回到“再造标签”的逻辑，必须避免：

### 1. 给第二层做完整命名体系

一旦第二层也有成套命名，就会变成“第二套人格系统”。

### 2. 让第二层拥有独立传播性

一旦第二层也能独立截图、独立分享，它就会直接变成新的标签卡。

### 3. 用文案直接宣判用户

例如：

- 你不敢承认这一面
- 你其实更在意别人怎么看你
- 你只是想拿一个好看的人设

这些都会迅速带来教育感和防御感。

### 4. 把反转做成剧情揭晓

如果第二层像彩蛋揭晓、隐藏结局或剧情翻转，用户会把它当作玩法，而不是结构。

META-TI 要的不是“哇，原来还有第二张”，而是：

`为什么第二张默认没被放出来？`

## 验收标准

这个界面规范是否成立，主要看以下四点：

### 1. 用户是否先认领了封面卡

如果用户第一反应不是“这张能发”，说明封面没有成立。

### 2. 用户是否会意识到底下压着东西

如果第二层出现后，用户只把它理解成“另一个结果”，说明留痕设计失败。

### 3. 用户是否能在不读长文案的情况下感知默认差异

如果必须靠解释才能明白封面和底稿的关系，说明界面没有承担表达任务。

### 4. 用户在分享时是否意识到自己在选版本

如果分享动作仍然只是“发结果”，说明导出区设计不成立。

## 最终要求

这套规范的目标不是让用户被教育。

而是让用户在一次很自然的浏览和点击中，自己得出一种模糊但明确的感觉：

`屏幕上这张最好看的卡，不一定是唯一结果；更像是被放到前面的那一层。`

当用户自己找到这一步，META-TI 才真正从“讲理念”变成“让理念发生”。
