# TMTI 2.0 方案

## 文档目标

这份文档只回答两件事：

1. 为什么当前版本仍然像一个普通的标签测试
2. 2.0 应该怎样通过扩展题库和重做结果结构，把理念真正变成体验

这不是概念复述。

这是给下一位落地 agent 的执行方案。

## 1. 当前版本的核心问题

当前线上版本已经有了几个正确方向：

- `TMTI` 作为表层品牌成立
- 默认先给出一张可传播的封面卡
- 页面里已经出现了底稿、留痕、导出选择等要素

但它仍然没有让用户真正感到：

`封面不是唯一结果，而是被推到前面的那一层。`

问题不在“有没有第二层”，而在“第二层没有被体验到”。

### 1.1 当前版本像普通测试的原因

#### A. 结果仍然是“先得出一个标签”

当前流程本质上仍然是：

- 计算四个轴
- 匹配一个 `publicType`
- 再附加一些底稿和留痕

这意味着主结构仍然是：

`结果 -> 注释`

而不是：

`多个可能版本 -> 一个被推上封面 -> 其余版本留下痕迹`

#### B. 第二层没有真正成为“残留”

现在的第二层虽然不再完全像另一张人格卡，但仍然过于完整。

它仍然有：

- 标题
- 稳定字段
- 相对完整的卡片边界

这会让用户把它理解成“另一张降级结果卡”，而不是“封面底下压着的内容”。

#### C. 筛选过程没有被看见

页面上虽然有 `candidatePool`、`cutWords`、`conflictEvidence`，但它们仍然是附属信息。

用户真正看到的中心仍然是：

- 一个名字
- 一段定义
- 一句副标题

于是他得到的仍然是“一个类型”，不是“一个被筛出来的版本”。

#### D. 页面太会解释自己

当前版本仍然有不少“解释玩法”的文案和结构提示。

这会让用户知道产品想表达什么，但不会自己走到那个感受。

2.0 的重点不是再解释，而是让界面自己完成表达。

## 2. 2.0 的目标

2.0 的产品目标只有一句：

`让用户实际感到封面是被筛出来的，而不是被测出来的。`

这句话意味着 2.0 必须同时做到：

1. 保留一张真能发的封面卡
2. 让用户看到封面不是唯一结果
3. 让被压下去的内容以“残留”的形式持续存在
4. 让分享动作显式变成“带走哪一层”的选择

## 3. 2.0 的核心体验

2.0 不再追求“结果揭晓”，而追求“封面定稿”。

用户体验路径应为：

`我来测一个标签 -> 页面先出现几个可能版本 -> 其中一个慢慢被定成封面 -> 我发现还有一些词和说法被裁掉了 -> 我在导出时必须决定要不要把它们一起带走`

重点不在“我是什么”，而在：

`为什么这一版被放到了最前面。`

## 4. 2.0 的结果结构

2.0 的结果结构不再是：

- Public Type
- Draft Card
- Trace Notes

而应改成四层：

### 4.1 候选池 Candidate Pool

至少保留 `3` 个接近结果，而不是只保留一个第一名。

它们不需要都有完整定义，但必须让用户看到：

- 不是只有一个版本曾经成立
- 页面做过选择

候选池不是给用户选人格，而是作为“封面定稿前的现场”。

### 4.2 封面结果 Cover Result

封面结果仍然是一张完整、可分享、可认领的主卡。

但它不再像“系统算出来的唯一答案”，而更像：

`从几个说得通的版本里，被推到最前面的那一个。`

### 4.3 残留层 Residue Layer

这不是第二张卡。

这是“没有进入封面的内容”。

残留层由三部分组成：

- 被裁掉的词
- 没被采用的描述
- 前后题目中的冲突痕迹

它应始终部分可见，不应该完全隐藏后再弹出。

### 4.4 导出预览 Export Preview

导出预览不是功能区，而是筛选过程的最后证据。

它必须明确表现出：

- 默认带走的是封面
- 残留层默认被裁掉
- 用户可以决定要不要连同裁切痕迹一起带走

## 5. 2.0 的页面重排

## 5.1 首页

首页要大幅去说明化。

保留：

- `TMTI`
- 主标题
- 开始按钮

删除：

- 当前用于解释玩法的三张说明卡
- 明说“剩下那层不会一开始被摆到封面”
- 明说“底稿默认不参与分享”

首页只保留一种模糊但成立的承诺：

`给你一张说得过去、也拿得出手的结果卡。`

## 5.2 问卷页

问卷页只做正常问卷，不泄露结构。

删除或隐藏：

- 所有面向用户可见的 `signalTags`
- 明说“别替自己选最漂亮的答案”这种提示

保留：

- 进度感
- 节奏感
- 轻微状态变化

问卷页最多只允许出现非常轻的系统状态词，例如：

- 已记录
- 已保留
- 已更新

不能出现：

- mirror
- exposure
- preference
- conflict

## 5.3 结果页

结果页是 2.0 的核心重构区域。

### 结果页新的主布局

桌面端采用三段式：

1. 上区：候选池 + 封面定稿
2. 中区：残留层
3. 下区：导出预览

移动端采用单列，但顺序一致。

### 结果页最关键的变化

#### A. 去掉主卡内部的大海报式“第二卡”

当前主卡里的海报视觉太像第二张卡，会干扰用户对残留层的判断。

2.0 中：

- 主视区只保留一个完整封面
- 导出预览里再出现海报式成品图

也就是说：

- 结果页看的是“定稿过程”
- 导出区看的是“成品”

#### B. 残留层必须始终可见

不要再做成完全被压住的整张卡。

改成：

- 封面旁边一条持续存在的词条残层
- 封面底边漏出的一段裁切短语
- 被划掉或没被采用的词云

用户不需要主动找“第二张卡”，因为第二层本来就一直在场。

#### C. 候选池要从装饰变成主结构

当前 `candidatePool` 只是几个小 chip，存在感太弱。

2.0 中应改成：

- 至少 3 个候选名字可见
- 封面结果以定稿态突出
- 其他候选保留“曾经成立过”的感觉

例如：

- 一个候选被放大为封面
- 另外两个淡出、缩小、偏移到旁边

这时用户会直观看到：

`它不是唯一答案，它是最后被选中的答案。`

#### D. 导出预览必须带裁切感

当前导出预览虽然已经有草稿层，但还不够像“被裁掉的部分”。

2.0 要明确让用户看到：

- 封面图是干净的成品
- 残留层默认被裁切掉
- 勾选“连同留痕一起导出”后，裁切部分才回到预览中

## 6. 2.0 的 reveal 机制

2.0 不再使用“到时间自动 reveal”的方式作为核心。

时间可以保留，但只是辅助，不是主触发。

真正的 reveal 应绑定用户动作：

- 停留
- 滑动
- hover
- 按压封面
- 打开导出预览
- 切换导出模式

### 6.1 TMTI 与 META-TI 的露出策略

`TMTI` 仍然是表层品牌。

`META-TI` 不应靠大标题切换出现。

2.0 中只允许：

- 在导出预览里轻微出现
- 在残留层边缘露出
- 在成品导出状态里出现小号字标

不允许：

- `TMTI / META-TI` 并排大标题
- 明说“现在进入 META-TI”
- 页面主动解释二者关系

## 7. 2.0 的题库扩展

当前题库数量偏少，虽然分类正确，但还不足以支持“候选竞争 + 封面筛选 + 残留层”。

2.0 建议把题库从当前规模扩展为：

- 主题库 `48` 题
- 每次呈现 `24` 到 `30` 题
- 保留随机抽题
- 保留镜像结构

### 7.1 题库目标

题库不只是为了算分，而是为了产出三类内容：

1. 封面词 Cover Words
2. 被裁词 Cut Words
3. 冲突证据 Conflict Evidence

如果一题只能贡献分数，不能贡献这三类结果之一，就不够好。

### 7.2 题库分组

建议扩成六组：

#### A. 公开认领题

测用户更愿意认领哪种公开说法。

输出：

- Cover Words
- 候选池偏好

#### B. 体面偏好题

测用户想保留什么样的体面。

输出：

- Cover Words
- Cover Subtitle 候选语气

#### C. 暴露回避题

测用户不想被放上封面的内容。

输出：

- Cut Words
- Residue 短语

#### D. 镜像冲突题

同一结构，前后两次换话术测量。

输出：

- Conflict Evidence
- 候选池排序扰动

#### E. 场景切换题

同一个人，在不同关系和场景下如何改口。

输出：

- 候选池竞争
- Residue 旁注

#### F. 导出倾向题

测用户更容易接受哪种成品感。

输出：

- 导出默认态提示
- Cover 排序加权

### 7.3 题库数量建议

每组建议：

- 公开认领题：10
- 体面偏好题：10
- 暴露回避题：10
- 镜像冲突题：8
- 场景切换题：6
- 导出倾向题：4

总量：

- 48 题

每次抽题建议：

- 每组最少抽到 3 题
- 镜像题成对出现

### 7.4 题库输出结构

每道题新增结构化输出，不再只有轴和分值。

建议新增字段：

- `coverSignals`
- `cutSignals`
- `conflictCue`
- `candidateHints`
- `exposureCost`

也就是说，题目不只是算一个轴，而是直接往结果素材池里塞内容。

## 8. 2.0 的数据结构调整

当前 `ResultSnapshot` 还不够支撑 2.0。

建议从：

- `publicType`
- `draftCard`
- `traceNotes`

改为：

- `candidatePool`
- `selectedCover`
- `coverWords`
- `cutWords`
- `conflictEvidence`
- `residueMarks`
- `traceNotes`
- `exportDefaultMode`

重点是：

`draftCard` 不再作为一个完整对象存在。

2.0 不需要“第二张卡”，只需要“残留物集合”。

## 9. 2.0 的文案收缩原则

2.0 不是完全不要文案，而是只保留必要的界面词。

保留：

- 结果
- 预览
- 发这一版
- 连同留痕一起导出
- 先留着

减少：

- 概念解释
- 理念说明
- 教学句式
- 自我总结型句子

原则：

`能靠层级表达的，不靠句子表达。`

## 10. 2.0 的实施顺序

建议按下面顺序推进，而不是同时大改。

### 第一阶段：修结果页结构

先做：

- 去掉主卡中的大海报式第二卡
- 重排结果页
- 让残留层持续可见
- 强化候选池
- 重做导出预览

在这个阶段，不急着扩题库。

### 第二阶段：扩题库

在结果结构跑通后，再把题库从当前规模扩到 `48`。

优先补：

- 镜像冲突题
- 暴露回避题
- 场景切换题

### 第三阶段：调 reveal

最后再把 reveal 从“时间驱动”为主，改成“用户动作驱动”为主。

这样可以避免边写结构边调 reveal，导致反馈混乱。

## 11. 2.0 的验收标准

2.0 是否成立，主要看下面四件事：

### 1. 用户是否能在不看说明文案的情况下感到“封面是被选出来的”

如果仍然只是“得到一个结果”，说明 2.0 失败。

### 2. 用户是否能明显看见残留层

如果用户还会说“我看不到第二张卡”，说明结构仍然没成立。

### 3. 用户是否会把底下那层理解成“被裁掉的内容”，而不是“另一个人格”

如果底层仍然被读成“另一个标签”，说明残留层设计失败。

### 4. 用户导出时是否明确意识到自己在选“带不带走裁切痕迹”

如果导出仍然只是“发结果”，说明 trap 没有闭环。

## 12. 最终执行要求

给下一位 agent 的任务，不是“继续优化一个有 meta 文案的人格测试”。

而是：

`把 TMTI 2.0 做成一个用户能亲眼看到封面被筛出来、而不是被测出来的产品。`

一句更硬的执行要求：

`删掉解释，扩大题库，让题目直接产出封面词、被裁词和冲突证据；重做结果页，让多个候选先竞争，再由一个版本定成封面。`
